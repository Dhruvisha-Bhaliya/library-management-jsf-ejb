# library-management-jsf-ejb
A Java EE application built using JSF, EJB, and JPA.  Features include CRUD operations on Books and Branches,  assigning books to branches, and displaying data in JSF pages.
